#lang scheme

(define (a-plus-abs-b a b)
((if (> b 0) + -) a b))

(a-plus-abs-b 3 -92)

;Description of the procedure: In the above procedure, the operator allows for the combination of the operand to return thesame value
; either if b is a negative number or a positive number. 